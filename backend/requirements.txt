fastapi==0.115.5
uvicorn[standard]==0.32.1
websockets==13.1
aiohttp==3.11.10
python-dotenv==1.0.1
# Numerical stack pinned to the versions that produced the validated
# iteration results (strategy_engineV2.py imports scipy.signal.find_peaks).
# Unpinned -> fresh installs (e.g. Codespaces) pull different numpy/scipy/
# numba builds and change UKF/RBPF/Kramers numerics on identical recordings.
numpy==2.4.3
scipy==1.17.1
numba==0.66.0
llvmlite==0.48.0
solders>=0.21.0
base58>=2.1.1
yfinance>=0.2.0
python-binance>=1.0.0