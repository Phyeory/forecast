fastapi==0.115.5
uvicorn[standard]==0.32.1
websockets==13.1
aiohttp==3.11.10
python-dotenv==1.0.1
numpy>=2.0
# ── PERFORMANCE-CRITICAL ─────────────────────────────────────────────────────
# strategy_engineV2.py JIT-compiles its RBPF/UKF/KDE/Kramers inner loops with
# numba.  These are NOT optional in practice: without numba the @njit decorator
# degrades to a no-op and the kernels run as interpreted scalar Python loops,
# which measured ~75x SLOWER end-to-end (a 900-candle recording goes from ~2.3s
# to >180s, turning a 1000-recording batch from minutes into many hours).
# scipy supplies find_peaks for HVN/barrier detection; the pure-Python fallback
# is both slower and less accurate on plateaus.
numba>=0.61
scipy>=1.11
# ─────────────────────────────────────────────────────────────────────────────
solders>=0.21.0
base58>=2.1.1
yfinance>=0.2.0
python-binance>=1.0.0
